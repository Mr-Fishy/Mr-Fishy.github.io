# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Evan Fish
email: evanjfish@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  My personal projects, posts, and other things I find interesting.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://mr-fishy.github.io" # the base hostname & protocol for your site, e.g. http://example.com
github_username: Mr-Fishy

# Build settings
theme: minima
plugins:
- jekyll-feed
- jekyll-seo-tag

# Github Pages Requirements
lsi: false # Disable LSI (Latent Semantic Indexing) for performance reasons.
safe: true # Set to true to ensure your site is safe for GitHub Pages.
source: ./src # Set the source directory for your Jekyll site.
destination: ./_site # Set the destination directory.
incremental: true # Disable incremental builds for simplicity.
highlighter: rouge # Use Rouge for syntax highlighting.
gist:
  noscript: false # Disable the noscript tag for Gist embeds.
kramdown:
  math_engine: mathjax # Use MathJax for mathematical typesetting.
  syntax_highlighter: rouge # Use Rouge for syntax highlighting.

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
exclude:
- README.md
